const router = require('express').Router();
const work = require('../controllers/work');

// Route: GET /workflows
// router.get('/', work.getAll);

// Route: POST /works
// router.post('/', work.create);

module.exports = router;
